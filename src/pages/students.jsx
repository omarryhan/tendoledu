import React from 'react';
import Layout from '../components/Layout';
import { STUDENTS_THEME } from '../constants';

export default () => (
  <Layout theme={STUDENTS_THEME} currentPage="students">
    <h1> Under Construction! </h1>
  </Layout>
);
