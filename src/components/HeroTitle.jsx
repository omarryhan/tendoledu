import styled from 'styled-components';
import { FONT_SIZES } from '../constants';


export default styled.h1`
    ${FONT_SIZES.xxxxxl}

    font-weight:bold;
    text-align: left;
    margin-top: 1vh;
    margin-bottom: 1vh;
`;
